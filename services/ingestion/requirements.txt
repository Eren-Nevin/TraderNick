sanic>=23.0
# defistream>=2.33.0 is REQUIRED: the Binance SPOT streams/backfills call
# raw_trades().market("spot") (and spot OHLCV), and `.market()` only exists in
# the 2.3x line. Do NOT downgrade below 2.33.0 — older wheels silently lack
# `.market()` and spot ingestion breaks with AttributeError. NB the package's
# `defistream.__version__` attribute under-reports (says 2.31.0 while the 2.33.0
# wheel is installed); trust `pip show defistream`, not the attribute.
defistream>=2.33.0,<3.0.0
clickhouse-connect[async]>=0.7.20
python-dotenv>=1.0
polars>=1.0
pyarrow>=14.0
httpx>=0.27
